# Leave unset to use the /api proxy defined in vite.config.ts during local dev.
# Set explicitly when pointing at a deployed backend, e.g.:
# VITE_API_BASE_URL=https://api.yoursite.com
VITE_API_BASE_URL=
